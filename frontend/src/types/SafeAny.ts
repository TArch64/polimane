/**
 * Use SafeAny when TypeScript's strict typing is not feasible or practical.
 * Prefer specific types when possible. This type should be used sparingly
 * and replaced with proper types when the structure becomes known.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type SafeAny = any;
