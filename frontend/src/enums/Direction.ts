export enum Direction {
  TOP = 'top',
  LEFT = 'left',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}

export const DirectionList = [
  Direction.TOP,
  Direction.LEFT,
  Direction.RIGHT,
  Direction.BOTTOM,
] as const;
