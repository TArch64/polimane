export enum EditorCursor {
  DEFAULT = 'default',
  GRAB = 'grab',
  GRABBING = 'grabbing',
  CROSSHAIR = 'crosshair',
}

export enum EditorCursorTarget {
  CANVAS = 'canvas',
  CONTENT = 'content',
}
